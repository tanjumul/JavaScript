"use strict " //This means , treat all code as a newer version of JavaScript 

// alert('Tanjumul again !')  // we are using Nodejs not browser you moreO.. 

//code readability should be there ! 
//ecma script is there for latest standards tc39 you know ! 

/** number , bigint , string , boolean , null 
 * null is a standalone value  which is also prefered as a object
 * boolean = true / false ; 
 * symbol = unique
 */

/**
 * non premitive ; 
 * objects 
 * 
 */

let newmachine = { 
  key : 'tanjumul' 
}

console.log(newmachine.key); 

console.log(typeof null ); // see null is a object 
console.log(typeof undefined ); 
